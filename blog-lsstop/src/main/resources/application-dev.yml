# 本地开发环境配置
# 配置端口
server:
  port: 8080
  servlet:
    session:
      timeout: 21600

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/new-blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: lishusheng7898
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true
    #SQL日志打印到控制台
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:/mapper/*.xml
