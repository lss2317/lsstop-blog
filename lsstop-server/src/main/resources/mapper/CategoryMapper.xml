<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsstop.mapper.CategoryMapper">

    <resultMap id="categoryVoResult" type="com.lsstop.domain.vo.CategoryVO">
        <id property="id" column="id" jdbcType="INTEGER" javaType="java.lang.Integer"/>
        <result property="categoryName" column="category_name" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result property="articleCount" column="article_count" jdbcType="INTEGER" javaType="java.lang.Integer"/>
    </resultMap>

    <select id="getCategoryList" resultMap="categoryVoResult">
        select c.id,
               c.category_name,
               count(a.id) as article_count
        from blog_category c
                 left join blog_article a on c.id = a.category_id and a.deleted_at = 0
        where c.deleted_at = 0
        group by c.id, c.category_name
        order by article_count desc, c.id
    </select>

</mapper>
